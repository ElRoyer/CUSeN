{
  "build": {
    "builder": "NIXPACKS",
    "config": {
      "NIXPACKS_APT_PACKAGES": ["libpq-dev"]
    }
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port 8000"
  }
}
